## 🔧 1. Create & Activate Virtual Environment

```bash
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

## 📓 2. Start Jupyter Notebook

```bash
jupyter notebook
```

This will open the Notebook UI in your browser.

---
